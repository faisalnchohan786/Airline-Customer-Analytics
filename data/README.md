# Data

Place the local airline SQLite database here:

```text
data/travel.sqlite
```

The database is intentionally excluded from version control because of its size.

The application will stop with a clear message if the database is missing.

Do not commit production, private, or credential-bearing data to this repository.
